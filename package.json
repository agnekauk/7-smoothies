{
  "name": "7-smoothies",
  "version": "1.0.0",
  "description": "Simple design to take a look into display:flex properties",
  "main": "index.js",
  "scripts": {
    "start": "dead-server --port=3000 --host=localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Agne",
  "license": "MIT",
  "devDependencies": {
    "dead-server": "^1.0.6"
  }
}
